using System;
using Neopocket.Utils;

namespace Neopocket.Forms
{
    /// <summary>
    /// Formulário de listagem dos relatórios
    /// </summary>
    public partial class FrmRelatorioLista : FormBase
    {
        #region [ Construtor ]

        public FrmRelatorioLista()
        {
            InitializeComponent();
        }

        #endregion

        #region [ Menu ]

        /* Pedidos */
        private void btnRelatorioPedidos_Click(object sender, EventArgs e)
        {
            Util.FormExibir(new FrmRelatorioPedido());
            this.Close();
        }

        /* Clientes */
        private void btnRelatorioClientes_Click(object sender, EventArgs e)
        {
            Util.FormExibir(new FrmRelatorioClienteCidade());
            this.Close();
        }

        #endregion
    }
}